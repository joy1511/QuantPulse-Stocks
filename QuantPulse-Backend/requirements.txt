# QuantPulse Backend Dependencies — Lean Build for Render Free Tier (512MB)
#
# STRATEGY: Only list packages that app code DIRECTLY imports.
# Transitive deps (e.g. langchain, opentelemetry) are auto-installed by pip.
#
# REMOVED (not imported / have graceful fallbacks):
#   tensorflow-cpu  — protobuf conflict with crewai; lstm_service returns Neutral
#   crewai-tools    — pulls selenium/docker/lancedb/embedchain; SerperDevTool has try/except
#   chromadb        — never loaded (memory=False on every Crew); pulls onnxruntime 200MB
#   litellm         — not imported; crewai installs it as transitive dep if needed
#   langchain-groq  — not imported directly
#   langchain-core  — not imported directly
#   groq            — not imported directly
#   tiktoken        — not imported directly
#   posthog         — not imported directly
#   rich            — not imported directly
#   nsepython       — pulls scipy 50MB; has try/except fallback; yfinance covers same data

# Core Infrastructure
fastapi==0.115.0
uvicorn[standard]==0.32.1
gunicorn==23.0.0
python-dotenv==1.1.1
pydantic==2.9.2
email-validator==2.3.0
httpx==0.27.2
cachetools==5.5.0
starlette==0.38.6

# Database & ORM
sqlalchemy==2.0.46
psycopg2-binary==2.9.11
alembic==1.18.4
pymongo==4.9.1
motor==3.6.0

# Authentication & Security
python-jose==3.5.0
passlib[bcrypt]==1.7.4
bcrypt==5.0.0
python-multipart==0.0.22
slowapi==0.1.9
authlib==1.6.8
itsdangerous==2.2.0

# Data & Math
yfinance==1.1.0
pandas==2.2.3
numpy==1.26.4
scikit-learn==1.6.0
hmmlearn==0.3.2
joblib==1.5.3

# NLP (news sentiment)
textblob==0.19.0

# AI Model Weights Download
huggingface-hub==0.26.5

# Agentic Framework (core only — transitive deps auto-installed by pip)
crewai==0.80.0

# Broker Integrations
smartapi-python>=1.3.5
dhanhq>=1.4.5
cryptography>=42.0.5

# Monitoring
psutil>=6.0.0
